name: "Hello Actions!"

on:
  workflow_dispatch:

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: "Echo Greeting"
        run: "echo 'Hei på deg Github Actions!'"
      - name: "Echo Goodnight"
        run: "echo 'Natta!'"
